{% extends 'base.html.twig' %}

{% block title %}Tous les utilisateurs{% endblock %}

{% block body %}
    <div class="container users">
        {% for user in users %}
            <p>{{user.email}}</p>
        {% endfor %}
    </div>
{% endblock %}
